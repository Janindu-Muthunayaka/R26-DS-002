# services/voice/requirements.txt
# Component 4 — Voice Interaction (Bumal)
# Runs in its OWN venv — do NOT install into the main system's environment.
#
# The main system pins numpy 1.26.4 and transformers 5.1.0 for measured
# reproducibility reasons (Chapter 4 cites results under those versions).
# Bumal's code uses a different stack.

fastapi>=0.100.0
uvicorn>=0.20.0

# NLLB translation model
transformers>=5.0.0
torch>=2.0.0
sentencepiece>=0.1.97

# Trained intent classifier
scikit-learn>=1.3.0
joblib>=1.3.0

# Online style learning
river>=0.21.0

# Interaction logging
tinydb>=4.7.0
